<header class="main-header">

    <div class="container">
        <nav class="navbar" id="myTopnav">
            <div class="icon-bar" onclick="Show()">
                <i></i>
                <i></i>
                <i></i>
            </div>
            <ul id="nav-lists">
                <li class="close"><span onclick="Hide()">×</span></li>
                <li><a class="<%= path === '/' ? 'active' : '' %>" href="/">Home</a></li>


                <li><a class="<%= path === '/products' ? 'active' : '' %>" href="/products">Books</a></li>


                <!-- only display this information if user is logged in -->
                <% if (isAuthenticated) { %>

                    <li><a class="<%= path === '/cart' ? 'active' : '' %>" href="/cart">Cart</a></li>


                    <li><a class="<%= path === '/orders' ? 'active' : '' %>" href="/orders">Orders</a></li>


                    <li><a class="<%= path === '/admin/add-product' ? 'active' : '' %>" href="/admin/add-product">Add
                            Book
                        </a></li>


                    <li><a class="<%= path === '/admin/products' ? 'active' : '' %>" href="/admin/products">Admin Books
                        </a></li>

                    <% } %>


                        <!-- only display this information if user is NOT logged in -->
                        <% if (!isAuthenticated) { %>
                     
                                <li><a class="<%= path === '/login' ? 'active' : '' %>" href="/login">Login</a></li>


                                <li><a class="<%= path === '/signup' ? 'active' : '' %>" href="/signup">Signup</a></li>
                                <% } else { %>
                                    <!-- show logout if user is logged in -->

                                    <form action="/logout" method="post">
                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                        <button class="topnav-right-button" type="submit">Logout</button>
                                    </form>

                                    <% } %>
                            
            </ul>

        </nav>
    </div>

</header>