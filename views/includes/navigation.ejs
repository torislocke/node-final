<header class="main-header">

    <nav class="topnav" id="myTopnav">
               <a class="cseCourse" href="https://node-application-one.herokuapp.com/"><i
                    class="fa fa-fw fa-home"></i>CSE341</a>


            <a class="<%= path === '/' ? 'active' : '' %>" href="/"><i class="fa fa-fw fa-shopping-bag"></i>Shop</a>


            <!-- <a class="<%= path === '/products' ? 'active' : '' %>" href="/products">Products</a> -->

   
            <!-- only display this information if user is logged in -->
            <% if (isAuthenticated) { %>

                <a class="<%= path === '/cart' ? 'active' : '' %>" href="/cart">Cart</a>


                <a class="<%= path === '/orders' ? 'active' : '' %>" href="/orders">Orders</a>


                <a class="<%= path === '/admin/add-product' ? 'active' : '' %>" href="/admin/add-product">Add
                    Product
                </a>


                <a class="<%= path === '/admin/products' ? 'active' : '' %>" href="/admin/products">Admin Products
                </a>

                <% } %>


                    <!-- only display this information if user is NOT logged in -->
                    <% if (!isAuthenticated) { %>
                        <div class="topnav-right">
                            <a class="<%= path === '/login' ? 'active' : '' %>" href="/login"><i
                                    class="fa fa-fw fa-user"></i>Login</a>


                            <a class="<%= path === '/signup' ? 'active' : '' %>" href="/signup">Signup</a>

                            <% } else { %>
                                <!-- show logout if user is logged in -->

                                <form action="/logout" method="post">
                                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                    <button class="topnav-right" type="submit">Logout</button>
                                </form>

                                <% } %>

                        </div>
                        <a href="javascript:void(0);" class="icon" onclick="myFunction()">
                            <i class="fa fa-bars"></i>
                        </a>
            
        </nav>

</header>